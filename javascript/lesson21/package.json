{
  "name": "lesson21",
  "version": "1.0.0",
  "description": "MyFreedom lesson 21 Project",
  "scripts": {
    "clean:output": "rimraf dist",
    "dev": "npm run clean:output && parcel index.html",
    "build": "npm run clean:output && parcel build index.html"
  },
  "author": "Olzhas",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.7"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.12.0",
    "parcel": "^2.12.0",
    "rimraf": "^5.0.5"
  }
}
